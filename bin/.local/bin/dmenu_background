#!/usr/bin/env bash
# script to set wallpaper through xwallpaper.

[[ -z $1 ]] && exit 1

SELECTED="$(echo -e "zoom\ntile\nstretch\nmaximize\ncenter\nfocus" | dmenu)"
[[ -z $SELECTED ]] && exit 1

xwallpaper --$SELECTED $1
exit 0
